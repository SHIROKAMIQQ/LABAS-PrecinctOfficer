# LABAS Precinct Officer

## Prerequisites

Copy what's in the `.env.sample` to your local `.env` file via the ff command:

```sh
cp .env.sample .env
```

## Developing

Run locally:

```sh
pnpm run dev
```

## Building

To create a production version of your app:

```sh
pnpm run build
```
